name: serial
description: Serial transport and binary packet parsing for model rocket telemetry.
version: 0.1.0

environment:
    sdk: ^3.13.1

dependencies:
    flutter_libserialport: ^0.6.0
    meta: ^1.11.0

dev_dependencies:
    test: ^1.24.0
