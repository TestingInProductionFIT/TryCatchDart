name: dead_reckoning
description: Ground-side dead reckoning estimation for model rocket telemetry (pure Dart, no Flutter).
version: 0.1.0

environment:
    sdk: ^3.13.1

dependencies:
    meta: ^1.11.0

dev_dependencies:
    test: ^1.24.0
